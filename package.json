{
  "name": "kodesiana-thesis-whale",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier . --write",
    "start": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "wrangler": "^4.26.0"
  }
}
