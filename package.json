{
  "name": "kodesiana-app-paus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier . --write",
    "start": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "wrangler": "^3.103.1"
  }
}
